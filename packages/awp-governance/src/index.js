/**
 * @awp/governance — shared, fail-closed gate enforcement.
 * See src/gates.js for the contract.
 */
export { checkGates, checkRuntimeMutation, resolveBlueprintId, REQUIRED_GATES } from "./gates.js"
